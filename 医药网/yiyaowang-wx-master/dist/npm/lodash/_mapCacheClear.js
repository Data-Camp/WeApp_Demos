"use strict";var exports=module.exports={};var Hash = require('./_Hash.js'),
    ListCache = require('./_ListCache.js'),
    Map = require('./_Map.js');

/**
 * Removes all key-value entries from the map.
 *
 * @private
 * @name clear
 * @memberOf MapCache
 */
function mapCacheClear() {
  this.size = 0;
  this.__data__ = {
    'hash': new Hash,
    'map': new (Map || ListCache),
    'string': new Hash
  };
}

module.exports = mapCacheClear;
